import { ConnectorDirective } from './connector.directive';

describe('ConnectorDirective', () => {
  it('should create an instance', () => {
    const directive = new ConnectorDirective();
    expect(directive).toBeTruthy();
  });
});
